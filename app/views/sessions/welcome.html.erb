<h1>Welcome</h1>
<%- flash.each do |name, message| %>
<p><i><%= "#{name}: #{message}" %></i></p>
<% end %>

<div><%=link_to "See events created events",user_path(current_user)%></div>
 <div><%=link_to "Create an event",new_event_path%></div>
